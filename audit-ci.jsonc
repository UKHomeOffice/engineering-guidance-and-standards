{
  // $schema provides code completion hints to IDEs.
  "$schema": "https://github.com/IBM/audit-ci/raw/main/docs/schema.json",
  "critical": true,
  "high": true,
  "moderate": true,
  "low": true,
  "report-type": "summary",
  "allowlist": [
    // https://github.com/advisories/GHSA-mh99-v99m-4gvg is used during build and would cause the build to fail on a
    // malicious PR, but does not enable DOS attacks on the site.
    "GHSA-mh99-v99m-4gvg"
  ]
}
